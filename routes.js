const express = require("express");
const router = express.Router();

// Rotas da API (placeholder para testes locais)
// O login local é feito via mock no login.js (usuario: admin / senha: admin123)

module.exports = router;
